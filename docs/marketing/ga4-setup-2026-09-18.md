# GA4 生产接入记录 — 2026-09-18

## 配置

- Google Analytics 账户：larksq / 69867224。
- 属性：OpenLinear Production / 554839266；创建时选择 China 报告时区、USD 币种。
- Web 数据流：OpenLinear Production Web / 15800430637。
- 生产域名：https://openlinear.qiaosun.me；Measurement ID：`G-WP59LSRZSE`。
- [实时报告](https://analytics.google.com/analytics/web/#/a69867224p554839266/realtime/overview)。
- Enhanced measurement 关闭；显式发送 page_view，不自动采集表单、搜索、下载或历史路由。
- `app_entry` 配置为每会话一次的关键事件，无默认金额；含义仅为点击打开应用。
- 事件范围自定义维度 CTA ID 对应 `cta_id`。

## 当前事件边界

| 事件 | 当前范围 |
| --- | --- |
| page_view | 同意后的生产首页或应用入口文档打开；不是所有 SPA 路由浏览 |
| cta_click | 固定入口按钮；cta_id 为 open_app 或 setup_guide |
| app_entry | 打开应用按钮点击；不是登录、注册、激活或支付成功 |
| sign_up / activation_completed / purchase | 未实现，仍记 NA |

访问者主动同意后才加载 Google tag。可从 Analytics preferences 撤回；撤回禁用测量并刷新。开发域名、localhost、非白名单路径/查询参数、带参数的私有 hash 链接不加载 tag。页面标题固定，referrer 仅保留 origin，UTM 限白名单格式；不发送邮箱、任务内容或 OAuth 参数。广告存储与个性化关闭。

这些是同意用户的测量数据，会受拒绝同意、拦截和设备差异影响；不能代表全量用户。

## 验收与发布

- 原生产基线：dpl_F8P4wRvQq8zfJcSHv3JdJvPHp5pD。
- 首次 Analytics 发布：dpl_GCR5xZcLZug5kZnNbu3tHdVrFuMQ，READY；仅替换 hosted.html、添加 public/openlinear-analytics.js，复用其余生产文件，未发布工作区其他未提交改动。
- Chrome 真实 QA 会话使用 `utm_source=qa&utm_medium=internal&utm_campaign=ga_setup_20260918`，同意后访问首页、点击本地指南和应用入口。
- GA4 Realtime 已显示 1 位活跃用户、OpenLinear Home 与 OpenLinear App 各 1 次浏览、page_view=2、cta_click=1。此为内部 QA，不作为自然获客结果。
- 初次 app_entry 未到达，补上普通同页点击的发送回调及 500ms 跳转兜底；保留新标签快捷键行为。修正发布：dpl_B7ym3KSn18tRtwExzMXFxVGHysGP，最终验证见下方补充。
- 本地 hosted 构建通过；6 项自动测试通过，覆盖同意前不采集、隐私路径排除、固定事件字段、撤回和点击跳转回退。

## 后续读数规则

- 9/18 之前的访问/点击依然 NA，不能回填零。启用当天为不完整日；首个完整采集日为 9/19，完整增长 T0 仍待注册/激活和新访客路径验收。
- 报表排除 source=qa 或 campaign=ga_setup_20260918；当前没有激活 IP 永久排除过滤器。先确认该测试来源归因已处理，再用于增长基线。其他开发/运营访问也必须标记与排除。
- 标准报告及新维度有处理延迟；实时验收不能作为成熟日数据。复盘记录截止时间、同意覆盖范围与样本数。
- 下次 9/22 09:00 复盘读取 GA4 属性，不以 Vercel Analytics 的 Enable/Demo Data 判定 GA4 未启用。Vercel Analytics 继续未启用。
- 注册、激活、D7、付费漏斗仍 NA，不以 app_entry 替代。

## 修正部署状态

2026-09-18 15:27 Asia/Shanghai：dpl_B7ym3KSn18tRtwExzMXFxVGHysGP 已 READY 并绑定生产域名。线上 JS 与本地逐字节一致，SHA-256 为 `ddcae887d46b712f09d230fed4f0995fb32b46a297d4f297227a8e3611e1df16`。Chrome 点击 Start building 成功进入已登录应用；未新建账号或业务记录。Realtime 的 cta_id 下钻确认 setup_guide=1。应用入口修正后的事件到达仍需等待报告刷新，不将配置成功等同于事件到达。

每周二/周五 09:00 的既有复盘自动任务已更新为 GA4 数据源与上述排除口径。
